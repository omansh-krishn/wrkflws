name: test
on: [push]

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      
      - name: Repository Dispatch
        uses: peter-evans/repository-dispatch@v3
        with:
          token: ${{ secrets.PAT }}
          repository: omansh-krishn/wrkflws
          event-type: new-release
          client-payload: '{"tagname": "v6.9"}'
